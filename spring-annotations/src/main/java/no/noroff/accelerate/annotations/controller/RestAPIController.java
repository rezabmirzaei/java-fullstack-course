package no.noroff.accelerate.annotations.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class RestAPIController {

    // A specialized (extended) version of @Controller.
    // Optimized for RESTful services.
    // Automatically serializes responses to JSON/XML.
    // Primarily used for building REST APIs.

}
